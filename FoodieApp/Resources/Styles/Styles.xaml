<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <x:Double x:Key="GlobalBodyFontSize">15</x:Double>
    <x:Double x:Key="GlobalCaptionFontSize">13</x:Double>
    <x:Double x:Key="GlobalHeadingFontSize">22</x:Double>
    <x:Double x:Key="GlobalSubheadFontSize">17</x:Double>

    <Style TargetType="ContentPage" ApplyToDerivedTypes="True">
        <Setter Property="BackgroundColor">
            <Setter.Value>
                <AppThemeBinding Light="{StaticResource PageBackground}"
                                 Dark="{StaticResource DarkBackground}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Switch" ApplyToDerivedTypes="True">
        <Setter Property="ThumbColor" Value="White" />
        <Setter Property="OnColor" Value="{StaticResource Primary}" />
    </Style>

    <Style x:Key="HeadingStyle" TargetType="Label">
        <Setter Property="FontSize" Value="{DynamicResource GlobalHeadingFontSize}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor">
            <Setter.Value>
                <AppThemeBinding Light="{StaticResource TextPrimary}"
                                 Dark="{StaticResource DarkTextPrimary}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SubheadingStyle" TargetType="Label">
        <Setter Property="FontSize" Value="{DynamicResource GlobalSubheadFontSize}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor">
            <Setter.Value>
                <AppThemeBinding Light="{StaticResource TextPrimary}"
                                 Dark="{StaticResource DarkTextPrimary}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BodyStyle" TargetType="Label">
        <Setter Property="FontSize" Value="{DynamicResource GlobalBodyFontSize}" />
        <Setter Property="TextColor">
            <Setter.Value>
                <AppThemeBinding Light="{StaticResource TextPrimary}"
                                 Dark="{StaticResource DarkTextPrimary}" />
            </Setter.Value>
        </Setter>
        <Setter Property="LineBreakMode" Value="WordWrap" />
    </Style>

    <Style x:Key="CaptionStyle" TargetType="Label">
        <Setter Property="FontSize" Value="{DynamicResource GlobalCaptionFontSize}" />
        <Setter Property="TextColor">
            <Setter.Value>
                <AppThemeBinding Light="{StaticResource TextSecondary}"
                                 Dark="{StaticResource DarkTextSecondary}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ErrorLabelStyle" TargetType="Label">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextColor" Value="{StaticResource ErrorColor}" />
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
        <Setter Property="TextColor" Value="White" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="FontSize" Value="{DynamicResource GlobalBodyFontSize}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="Padding" Value="20,14" />
        <Setter Property="HeightRequest" Value="50" />
    </Style>

    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{StaticResource Primary}" />
        <Setter Property="BorderColor" Value="{StaticResource Primary}" />
        <Setter Property="BorderWidth" Value="1.5" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="FontSize" Value="{DynamicResource GlobalBodyFontSize}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="Padding" Value="20,14" />
        <Setter Property="HeightRequest" Value="50" />
    </Style>

    <Style x:Key="CardStyle" TargetType="Frame">
        <Setter Property="CornerRadius" Value="14" />
        <Setter Property="HasShadow" Value="True" />
        <Setter Property="Padding" Value="14" />
        <Setter Property="Margin" Value="0,0,0,10" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BackgroundColor">
            <Setter.Value>
                <AppThemeBinding Light="{StaticResource CardBackground}"
                                 Dark="{StaticResource DarkCard}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InputEntryStyle" TargetType="Entry">
        <Setter Property="FontSize" Value="{DynamicResource GlobalBodyFontSize}" />
        <Setter Property="TextColor">
            <Setter.Value>
                <AppThemeBinding Light="{StaticResource TextPrimary}"
                                 Dark="{StaticResource DarkTextPrimary}" />
            </Setter.Value>
        </Setter>
        <Setter Property="PlaceholderColor" Value="{StaticResource TextHint}" />
    </Style>

    <Style x:Key="InputEditorStyle" TargetType="Editor">
        <Setter Property="FontSize" Value="{DynamicResource GlobalBodyFontSize}" />
        <Setter Property="TextColor">
            <Setter.Value>
                <AppThemeBinding Light="{StaticResource TextPrimary}"
                                 Dark="{StaticResource DarkTextPrimary}" />
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>